import * as React from "react"

/** @jsx jsx */
import { css, jsx } from "@emotion/core"

const List: React.FC = ({ children }): React.ReactElement => {
  return <div>{children}</div>
}

export default List
